# Homepage Configuration
#
# The `demos` section (quick_start + categories) is populated at load time
# from the demo/ directory on disk — see extensions/web/site/src/homepage/demo_scanner.rs.
# Only `title` and `subtitle` here are authoritative; anything else is overwritten.

hero:
  title: "Stop renting AI."
  title_highlight: "Own the system."
  subtitle: "This is a live demo of systemprompt.io, the only AI infrastructure you actually own. Ask the terminal below to block a leaked secret, build a spend dashboard, or audit its own session — type / for the full demo menu. Then create an account with a passkey, take $1 of credit, and run Claude Desktop through a gateway that answers to you: your policies, your audit trail, your bill."
  eyebrow: "Live demo · Five steps · $1 credit included"
  cta: "Start the demo"
  cta_url: "/register"
  cta_secondary: "See the five steps"
  cta_secondary_url: "#get-started"

demos:
  title: "Demo Catalogue"
  subtitle: "Every script below is a real, runnable shell script in demo/. Click a card to see the commands it runs, then copy them into your terminal."
