# Homepage Configuration
#
# The `demos` section (quick_start + categories) is populated at load time
# from the demo/ directory on disk — see extensions/web/site/src/homepage/demo_scanner.rs.
# Only `title` and `subtitle` here are authoritative; anything else is overwritten.

hero:
  title: "Own how your organization"
  title_highlight: "uses AI."
  subtitle: "systemprompt.io is self-hosted AI governance in a single Rust binary. Register with a passkey, claim $5 of credit to try it with Claude Desktop, and install the Systemprompt Bridge — your Claude tools connect through a gateway you own, with policy, audit, and cost attribution on every request."
  eyebrow: "systemprompt.io · Open Gateway"
  cta: "Get started"
  cta_url: "/register"
  cta_secondary: "Sign in"
  cta_secondary_url: "/login"

demos:
  title: "Demo Catalogue"
  subtitle: "Every script below is a real, runnable shell script in demo/. Click a card to see the commands it runs, then copy them into your terminal."
